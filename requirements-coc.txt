# loom dev-container distribution source (py) — generated INTO consumer templates by /sync-to-use; do not hand-edit the consumer copy. Registry host/org are ecosystem-relative, substituted at /sync-to-use distribution.
# Kailash COC baked framework set — template-owned, regenerated on /sync.
# Lighter five frameworks + hook formatters. ML/Align (torch-class, multi-GB) are
# OPT-IN via requirements-coc-ml.txt (compose `ml` profile) per plan §0 decision 1.
# Pins resolved 2026-07-19 (PyPI-verified latest per-package via PyPI JSON API; uv pip compile exit 0). Implements specs/dev-container-image.md §5.
# (Top-level exact pins; --generate-hashes supply-chain lock is the hardening follow-up
#  noted in the M1 verification record.)
kailash==2.55.0
kailash-dataflow==2.18.0
kailash-nexus==2.12.1
kailash-kaizen==2.37.0
kailash-pact==0.15.0
ruff==0.15.14
black==26.5.1
