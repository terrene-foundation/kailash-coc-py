# /sweep — 2026-05-31

Project-scoped repo-wide sweep across all 7 lanes after F4 dev-container shipped. Active workspace: `dev-container`.

## Top-line

- **F4 closed end-to-end** (PR #32 merged at `9cf7e8d`, fix at `6312a2a`, `terrenefoundation/kailash-coc-py:1.10.1` live, Overview synced).
- **0 CRIT, 0 HIGH new findings.** 2× MED stale-duplicate cleanup applied inline; 1× MED partial-closure surface on issue #28; 3× LOW stale local branches.
- Sweep 5 N/A (orchestration mode — no `tools/sweep-redteam.py`).

```
<!-- sweep-redteam:v1:N/A reason=orchestration-mode no_specs=false no_tool=true -->
```

## Findings

### [MED] [Sweep 1] active/03 + active/04 todos are stale post-shipping

**Location:** `workspaces/dev-container/todos/active/03-devcontainer-postcreate.md` + `workspaces/dev-container/todos/active/04-overlay-stubs-docs.md`

**Disposition:** **FIX-NOW — DELETE** (work shipped, closure receipts in `todos/completed/` confirm).

**Evidence:** M3 (devcontainer + postCreate) and M4 (overlay stubs) both have closure receipts at `todos/completed/03-…` and `todos/completed/04-…` — verified files match the shipped substrate (`.devcontainer/postCreate.sh`, `Dockerfile.user.example`, `compose.override.yml.example`, `requirements-user.txt`). The active/ versions are shard plans that should have been archived when work moved to completed/. Loom sync revert cycle (2026-05-30) re-surfaced them as untracked; commit 1 (`cf615c0`) committed both rather than just completed/.

**Why this matters:** Active todos signal "in-flight work" to `/sweep`, `/ws`, and the next session's read-on-resume. Leaving them in active/ falsely inflates the forest ledger.

**Action taken:** FIX-NOW inline — `git rm` both files in the post-sweep cleanup commit.

---

### [MED] [Sweep 1] active/05 — cross-repo /codify proposal — STILL-WANTED

**Location:** `workspaces/dev-container/todos/active/05-cross-repo-followup.md`

**Disposition:** **STILL-WANTED with re-validated value-anchor.** Queued; cannot complete in this repo.

**Value-anchor:** Brief — "the base image is repo-template-owned (regenerated on /sync), but the user's overlay is project-owned and survives /sync" + `specs/sync-ownership.md` §Landing path G1–G4.

**Evidence:** Pre-pivot M5 scope was loom-shipping the Dockerfile itself; the Architecture B pivot (2026-05-28, journal/0003) shrunk this dramatically. Post-pivot scope: a `/codify` proposal asking loom to (a) ship the consumer-shipped pointer configs (`docker-compose.yml`, `.devcontainer/devcontainer.json`, `bin/dev`, `.dockerignore`, `Dockerfile.user.example`, `compose.override.yml.example`, `requirements-user.txt`, `.devcontainer/postCreate.user.sh.example`, `DOCKERHUB.md`, `.github/workflows/{publish-dev-image,docker-build}.yml`) as template-owned files, and (b) declare preserve-globs (`*-user.*`, `*.user.*`, `compose.override.yml`, `Dockerfile.user`) so consumer overlays survive `/sync` regeneration.

**Why this matters:** Without the proposal, every downstream USE-template consumer must hand-port these files. The proposal is the structural pathway to "drop the COC template into a fresh project and get the dev container for free."

**Action taken:** Surface only — `/codify` proposal authoring is the next-session deliverable. Not in this sweep's scope.

---

### [MED] [Sweep 3] Issue #28 partially closed by loom sync `b2aec80`

**Location:** `https://github.com/terrene-foundation/kailash-coc-py/issues/28`

**Disposition:** **FILE-ISSUE-COMMENT** — recommend updating issue #28 with partial-closure note + b2aec80 SHA reference. Do NOT close the issue.

**Evidence:** Issue #28 enumerates 3 gaps:

1. `/prompts:<name>` deprecated — STILL OPEN (`.codex/prompts/` still emitted)
2. `.codex/developer-instructions/` not emitted — STILL OPEN (`ls .codex/developer-instructions/` → no such directory)
3. `bin/coc` + `bin/coc-<phase>` not shipped — **CLOSED** by `b2aec80` (bin/coc + 22 bin/coc-\* symlinks now present)

Re-verified 2026-05-31. F5 in the forest ledger tracks the same 3-gap surface; this is the issue counterpart.

**Why this matters:** Per `rules/git.md` § Issue Closure Discipline, partial deliveries should be commented with SHA references so the issue body stays an accurate work surface. Leaving issue #28 unchanged hides the partial progress.

**Action taken:** Surface only — comment authoring deferred to user (small, externally-visible write).

---

### [LOW] [Sweep 4] Three stale local branches

**Location:** local-only (not on origin):

- `chore/cross-repo-perm-grant-2026-05-09` (22d old, last commit `f3aa927`)
- `chore/sync-loom-7bbc13b8-2026-05-09` (22d old, last commit `cce24dc`)
- `codify/jack-hong-2026-05-28` (5d old, last commit `7e3f1be` — pre-loom-sync main)

**Disposition:** **DEFER-WITH-REASON.** All three are local-only; not blocking any remote work. Two chore/ branches likely have work already on main via subsequent /sync commits; codify branch was the "journal-writing checkout" the session-notes trap referenced.

**Why this matters:** Stale branches accumulate; they're cleanup candidates but not gates. `git branch -D` is reversible via reflog.

**Action taken:** Surface only — cleanup is operator discretion.

---

### [LOW] [Sweep 7] TODO marker in maintenance script

**Location:** `scripts/maintenance/refactor-pythoncode-to-functions.py:174`

**Disposition:** **FALSE-POSITIVE.** Marker is INSIDE a string literal that the refactor tool emits as boilerplate in its output (`func_lines.append("    # TODO: Verify return value")`). Not production code; the tool generates code with the marker for human review post-refactor.

**Action taken:** none.

---

## Sweep-level summary

| Sweep | Lane                         | Findings        | Disposition                                           |
| ----- | ---------------------------- | --------------- | ----------------------------------------------------- |
| 1     | Active todos                 | 3 active, 2 MED | 2× FIX-NOW (delete stale); 1× STILL-WANTED            |
| 2     | Pending journal entries      | 0               | clean — no `.pending/` dir                            |
| 3     | GH open issues (this repo)   | 1 open, 1 MED   | partial-close comment recommended (#28)               |
| 4     | Open PRs + stale branches    | 0 PRs, 3 LOW    | local cleanup discretion                              |
| 5     | Redteam gaps                 | N/A             | orchestration mode (sentinel logged)                  |
| 6     | Workspace + worktree hygiene | 0               | clean — no stale .session-notes, no worktrees         |
| 7     | Process hygiene              | 1 LOW, FP       | 0 uncommitted, in-sync with main, false-positive only |

## Cross-cutting observations

1. **F4 substrate is institutionally complete** — image published, Overview synced, publish pipeline self-driving on tag-push. Remaining work is exclusively cross-repo (F5 / loom#385 / issue #28).
2. **The loom sync revert cycle (2026-05-30) left two cleanup residuals** — active/03 and active/04 duplicates. Both fixable in one delete commit; underlying recovery via `~/.claude/projects/` transcripts was lossless on the substantive content.
3. **Sweep-5 N/A is correct for this repo class** — kailash-coc-py is a USE template, not a BUILD repo. The `tools/sweep-redteam.py` mandate applies to kailash-py / kailash-rs only. Sentinel logged so future readers can grep.
4. **Issue #28 is the externally-visible counterpart of F5** — once loom#385 ships the emitter fix, both close together. Tracking divergence between issue body (stale on bin/coc) and current state is a perpetual sweep concern; recommend the partial-close comment to drift-proof the issue.

## Recommended next-session items (ranked)

1. **File `/codify` proposal for cross-repo dev-container sync** (M5 in the active forest) — the structural pathway to make this work available in every downstream USE template, not just this one. Cannot complete this session; the proposal lands here, loom Gate-1 reviews, `/sync` distributes.
2. **Comment on issue #28** noting `bin/coc` partial closure at SHA `b2aec80`; keep open for parts 1+2 pending loom#385.
3. **Land loom#385** in a separate loom session (Codex emitter — `/prompts` deprecation + `.codex/developer-instructions/` emission). Closes F5 and parts 1+2 of issue #28 in one shot.
4. **Operator-discretion branch cleanup** — `git branch -D chore/cross-repo-perm-grant-2026-05-09 chore/sync-loom-7bbc13b8-2026-05-09` after confirming the work landed via /sync commits.
5. _(Optional)_ Add `tools/sweep-redteam.py` to this repo if/when domain specs grow beyond the dev-container workspace — would let Sweep 5 actually run instead of N/A-ing.

## Receipts

- F4 closure: PR #32 merge `9cf7e8d`; v1.10.1 image at digest `sha256:43944a514245fbb0959d861…`; Overview sync run `26702219428`.
- Boolean-coercion fix: `6312a2a`.
- F3 archival: inline header on `05-codify/01-substrate-gap-finding.md` (in commit `cf615c0`).
- F72 ratification: this conversation merged 5 commits via the fresh-substrate-adopter branch path, no `--no-verify` used.

```
<!-- sweep-redteam:v1:N/A reason=orchestration-mode no_specs=false no_tool=true -->
```
