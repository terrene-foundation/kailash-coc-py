# COC Sync Marker — kailash-coc-py (multi-CLI: claude + codex + gemini)
# Generated by coc-sync (Gate 2) — 2026-05-10 multi-CLI baseline recovery cycle.
# Closes the per-template parity gap: 2026-05-09 distribution shipped commands/sweep.md
# but skipped the AGENTS.md/GEMINI.md re-emission for the multi-CLI target. This cycle
# (a) re-distributes commands/sweep.md (byte-identical to loom HEAD 9b7da3c5) and
# (b) re-emits AGENTS.md (59558B) + GEMINI.md (59567B) from current loom HEAD via
# `node .claude/bin/emit.mjs --all --lang py`, ensuring MUST-4 of verify-resource-existence
# (Convergence/Round-Verdict Receipts, added 2026-05-09) lands in both multi-CLI baselines.

timestamp: 2026-05-10T06:15:00Z
source: loom/.claude
loom_version: "2.29.0"
loom_sha: "9b7da3c5"
template_version: "1.6.0"
template_type: multi-cli
variant: py
clis: [claude, codex, gemini]
stats:
  updated: 3          # commands/sweep.md + AGENTS.md + GEMINI.md
  created: 0
  unchanged: ~485
  composed: 0
  preserved: 3        # CLAUDE.md + .codex/local-config.toml + .gemini/local-settings.json
  baselines_emitted:
    cc: 0             # CLAUDE.md preserved (template-owned)
    codex: 1          # AGENTS.md re-emitted (closes MUST-4 absence gap)
    gemini: 1         # GEMINI.md re-emitted (closes MUST-4 absence gap)
  baseline_emission_bytes:
    block_cap_bytes: 61440
    warn_cap_bytes:  32768
    codex:
      rules: 10
      bytes: 59558
      tier: WARN
      headroom_bytes: 1882
      headroom_pct: 3.06
    gemini:
      rules: 10
      bytes: 59567
      tier: WARN
      headroom_bytes: 1873
      headroom_pct: 3.05
  cli_artifacts:
    codex:
      prompts: null   # not refreshed this cycle (no commands/agents/skills delta beyond sweep.md)
      skills: null
    gemini:
      commands: null
      skills: null
      agents: null
  mcp_guard:
    policies_populated: false   # still gated — runtime predicates unwired
verify:
  hook_integrity: ok
  obsoleted_purge_complete: ok
  build_path_leaks: 0
  emit_validators: pass         # validators 12/13/14 PASS per emit.mjs --all --lang py
  must4_presence: 1/1           # AGENTS.md + GEMINI.md each contain "Convergence / Round-Verdict"
proposal_status: distributed

# Why this cycle (gap analysis):
#   - 2026-05-09 distribution added MUST-4 (Convergence/Round-Verdict Receipts) to
#     verify-resource-existence.md at loom commit d4e62a8.
#   - Same-day /sync rs cycle correctly re-emitted AGENTS.md/GEMINI.md to kailash-coc-rs;
#     the parallel /sync py cycle distributed sweep.md but did NOT re-emit the multi-CLI
#     baselines on kailash-coc-py — root cause: per-template baseline-emission step was
#     selective on the py axis, not enumeration-driven.
#   - Empirical signal at audit time (2026-05-10): grep -c on MUST-4 returned 1 for rs
#     baselines and 0 for py multi-CLI baselines. This cycle closes the gap.
#   - Structural defense: per coc-sync.md Step 6.5, baseline emission MUST run for every
#     template in repos.<target>.templates whose baseline_files extends beyond [CLAUDE.md].

# Prior cycle (preserved for audit):
# 2026-05-09T20:45:00Z @ loom dfd9ffca — skill-audit cleanup distribution from loom 2.29.0
#   (loom PRs #111 + #112): 11 naive skills + 9 variant overlays + 3 sub-files + 7 cross-ref
#   edits + 2 extracts. Multi-CLI baseline re-emission was NOT performed in that cycle.
