# COC Sync Marker — kailash-coc-py (py lane, multi-CLI)
timestamp: 2026-08-17T14:55:00Z
source: loom/.claude
loom_sha: a44083a5261ed162bc456715d4b73e7e506566a4
variant: py
visibility: public
clis: ["claude", "codex", "gemini"]
base_sha: 404528457aaab4a68b321070a35d4229097657fd
stats:
  engine_verify: "CONSISTENT — 2689 in-plan files checked, every one matches expected; no obsoleted path present (sync-tier-aware --target py --template kailash-coc-py --verify, exit 0)"
  changed_paths_total: 351 # git status --porcelain -uall (NOT the bare form — it collapses untracked dirs); counted AFTER this marker + VERSION were written
  changed_by_prefix:
    .claude: 268
    .coc: 40
    .codex: 31
    .gemini: 9
    root: 2 # AGENTS.md, GEMINI.md
  purged: 0 # engine purge; see removal_review for the .coc lane-fate deletions
  version_stamp: "loom-tracking -> 2.45.0 @ a44083a5 (stamp-template-version --write --worktree <wt> --loom-root <src-worktree> --repo terrene-foundation/kailash-coc-py); own version 1.22.0 -> 1.23.0"
  hooks_resolving: "41/41"
  settings_pii_scan: "clean — 0 /Users/ or /home/ hits in .claude/settings.json"
  baselines_emitted:
    cc: 0 # CLAUDE.md is template-owned, preserved not emitted
    codex: 1 # AGENTS.md
    gemini: 1 # GEMINI.md
  baseline_emission_bytes:
    block_cap_bytes: 65536
    warn_cap_bytes: 32768
    codex: 57801 # 11 rules, WARN tier (steady state per v6 §2.2), headroom 11.8%
    gemini: 57801 # 11 rules, WARN tier, headroom 11.8%
    headroom_advisory: "11.8% is INSIDE the 15% proximity band — the next baseline MUST addition requires paired extraction OR a named-rationale exception per rule-authoring.md Rule 10. Floor is 10%; this cycle did not breach it (emit.mjs exit 0 under default strict-headroom)."
  source_freshness: "DISTRIBUTED FROM A DETACHED loom WORKTREE AT origin/main (sync-flow.md §0b(i)). The operator's main checkout was 2 commits BEHIND origin/main and check-sync-freshness --loom exited 1; distributing from it would have shipped the pre-#1786 .claude/guides/deterministic-quality/08-cross-sdk-parity.md, which carried an org-qualified `terrene-foundation/kailash-py` slug that #1786 genericized to `<org>/kailash-py`. Verified present in the source worktree before staging. --loom-root was passed to the stamper per §0b(iii), so loom_sha names THIS source, not the stale checkout."
  multi_cli_reemit: "sync-completeness Rule 8 FULL re-emit. emit-cli-artifacts --target py -> codex prompts=42 skills=40 agent-prompts=22, gemini commands=42 skills=40 agents=22, rules-reference skill codex=1 gemini=1 (81 path-scoped rules indexed, up from 79 last cycle); emit-coc --target py --lane use -> 203 artifacts, coc.version 1.0.0, 7 withheld by lane fate; emit.mjs --all --lang py -> AGENTS.md + GEMINI.md, exit 0."
  placement_correction: "emit-cli-artifacts --out <dir> writes UNDOTTED <dir>/codex + <dir>/gemini staging trees; it does NOT write the deployed .codex/ + .gemini/. Pointing --out at the worktree therefore leaves the DEPLOYED trees STALE while creating two stray root dirs. Caught by a discriminating check: the rules-reference index rose 79 -> 81 rules at emit, yet `git status` showed .codex/.gemini with ZERO changes — a contradiction that can only mean the deployed tree was untouched. Corrected per coc-sync.md Step 6.6: emit to a scratch dir, then copy the emitter-owned subtrees into .codex/ + .gemini/. Post-correction the two trees show 31 + 9 changed paths and carry the new orchestrator-context-economy rule."
  idempotency_check: "an INDEPENDENT emit into a scratch dir diffed against the worktree: .codex/prompts 0, .codex/skills 0, .gemini/commands 0, .gemini/skills 0, .gemini/agents 0, .coc 0 differing files; AGENTS.md + GEMINI.md byte-identical. CONTROL: appending one line to a scratch prompt file made the same diff report 1 differing file, so the check discriminates rather than reporting 0 unconditionally."
  removal_review: "5 deletions, ALL adjudicated to declared use_exclude: entries (sync-completeness pre-merge delivered-removal review) — .coc/commands/TEST-HARNESS-PROBE.md, .coc/skills/TEST-HARNESS-PROBE.md (commands/test-harness-probe.md + skills/test-harness-probe/** are BUILD-CC-only), .coc/rules/COC-ARTIFACT-EVAL-COVERAGE.md, .coc/rules/CROSS-SDK-INSPECTION.md, .coc/rules/DOCUMENTATION.md. No untraceable row."
  scaffold: ".claude/codex-mcp-guard -> ../.codex-mcp-guard symlink resolves; bin/coc byte-identical to loom template, mode 0755; 28 per-phase shims == 28 loom wrapper schemas; .codex/config.toml + .codex/hooks.json + .gemini/settings.json refreshed from the loom template trees (zero diff — already current)."
  codex_mcp_guard_tree: "NOT hand-copied this cycle, deliberately. loom's OWN .codex-mcp-guard/package.json carries repository.url https://github.com/<a-real-org>/loom.git while THIS template already carries the genericized https://github.com/<owner>/<repo>.git — so the Step-6.6 `cp -r` would have REGRESSED the template to the org-qualified value. The engine already owns this tree via the manifest `paths: .codex-mcp-guard/**` entry and --verify reported CONSISTENT. Three target-side files (package-lock.json, test-server.mjs, test-extract-policies.mjs) plus extract-policies.dump.json are destination-authored and were preserved. Surfaced to loom as a source-side scrub item."
  settings_reconcile: "Gate-2 automatic (in-worktree, post-engine-apply): deny rewrote 0 / deduped 0 / restored 0; hooks pruned 0, ADDED 2 (dispatch-contract-guard.js, delegation-default-guard.js)."
  disclosure_gate: "scan-synced-disclosure.mjs --check — loom SOURCE tree 4330 files scanned / 0 findings, exit 0; staged worktree 3644 files scanned / 0 findings, exit 0. Both counts are NON-ZERO, so neither run is a vacuous no-op pass."
  disclosure_delta: "This delivery introduces NO new org-qualified slug. An independent grep found org-slug residuals in .codex/prompts/cross-repo-authorize.md, .codex/skills/01-core-sdk/runtime-lifecycle.md, the .gemini mirrors, .coc/rules/ENV-MODELS.md, .coc/rules/CI-COST-DISCIPLINE.md and .coc/commands/CROSS-REPO-AUTHORIZE.md — every one of them PRE-EXISTS at this template's base commit and the hit COUNT is unchanged by this sync. They originate in loom's own committed corpus and are a loom-source scrub item, not a Gate-2 regression."
  sdk_pins:
    disposition: "HELD — all pins unchanged this cycle."
    reason: "The BUILD-side half of the pin refresh reads kailash-py's pyproject.toml, and this session holds NO cross-repo authorization for kailash-py (write receipts cover only kailash-coc-py, kailash-coc-rs, kailash-coc-claude-rs). Reading BUILD without a receipt is BLOCKED per repo-scope-discipline.md, so the pins were HELD rather than guessed. This is a DECLARED GAP, not a verified no-op: whether BUILD has since declared newer versions was NOT determined."
    install_surface: "requirements-coc.txt — kailash 2.62.0, kailash-dataflow 2.19.1, kailash-nexus 2.15.0, kailash-kaizen 2.45.0, kailash-pact 0.18.0 (unchanged)"
    ml_overlay: "requirements-coc-ml.txt — kailash-ml 2.2.2, kailash-align 0.7.4 (unchanged)"
    install_verify: "uv pip compile requirements-coc.txt -> exit 0, 74 packages resolved, 0 warn/error/conflict; requirements-coc-ml.txt -> exit 0, 122 packages. CONTROL: the same command against a synthetic kailash==99.99.99 pin exits 1 ('your requirements are unsatisfiable'), so exit 0 is a real resolution and not an instrument that cannot fail. `uv sync` is N/A for this template (no pyproject.toml)."
  inbox: ".claude/.proposals/inbox/ already present (README.md + an existing consumer entry) — additive ensure-exists produced zero diff."
